## Agent skills

### Issue tracker

Issues are tracked using local markdown files under `.scratch/`. PRs are not applicable. See `docs/agents/issue-tracker.md`.

### Triage labels

Triage labels use the default vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

The repository uses a single-context layout for domain docs. See `docs/agents/domain.md`.
